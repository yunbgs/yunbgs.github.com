<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: test111 | 云办公室]]></title>
  <link href="http://yunbgs.github.io/blog/categories/test111/atom.xml" rel="self"/>
  <link href="http://yunbgs.github.io/"/>
  <updated>2013-05-31T15:10:09+08:00</updated>
  <id>http://yunbgs.github.io/</id>
  <author>
    <name><![CDATA[yunbgs.com]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[不同机器上配置发Blog]]></title>
    <link href="http://yunbgs.github.io/blog/2013/05/23/work-test/"/>
    <updated>2013-05-23T14:17:00+08:00</updated>
    <id>http://yunbgs.github.io/blog/2013/05/23/work-test</id>
    <content type="html"><![CDATA[<p>想工作机器与家中机器上都可以同步发Blog,解决办法：</p>

<h1>1.同前配置</h1>

<p>安装和设置Git
安装Ruby环境</p>

<h1>2.不同配置</h1>

<p>获取自己github上的octopress仓库</p>

<pre><code>git clone -b source git@github.com:username/username.github.com.git octopress
cd octopress
git clone git@github.com:username/username.github.com.git _deploy
</code></pre>

<p>安装依赖gems</p>

<pre><code>gem install bundler
bundle install
rake install
或
bundle exec rake install
</code></pre>

<p>更新_deploy和source内容</p>

<pre><code>cd _deploy
git pull origin master
cd ..
git pull origin source
</code></pre>

<p>至此，我们在另外一台pc上的octopress环境就搭建完成了，以下是比较重要的说明： 试想一下，如果我们此时在这个环境中执行：</p>

<pre><code>rake new_post['title']
rake generate
rake deploy
</code></pre>

<p>会发生什么呢？显然，我们所有的文章只剩下刚刚新建的这个了。所以我们需要做的是将旧的<em>post目录下的文章同步到新的</em>post目录下，这就是git的用法了。</p>

<p>进入旧的_post的目录下：</p>

<pre><code>git add *
git commit -m
git push origin source
</code></pre>

<p>每次写文章之前，进入当前的_post目录下：</p>

<pre><code>git pull origin source
</code></pre>

<p>写完之后，执行：</p>

<pre><code>git add *
git commit -m
git push origin source
</code></pre>

<p>也就是说，每次写之前写之后都要与仓库进行同步！</p>
]]></content>
  </entry>
  
</feed>
